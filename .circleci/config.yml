version: 2
jobs:
  build:

    working_directory: "/go/src/github.com/matzhouse/example_go_circle"

    docker:
      - image: golang:1.9

    steps:
      - checkout

      - run: ls -lart && pwd
      - run: go test -v ./...
